import React from 'react'
// import { PhotoData } from '../../data/PhotoData'
import './Photos.scss'

export default function Photos() {
  return (
    <div>
      {/* {PhotoData.map((pic) => {
        return <img key={pic.id} src={pic.image} alt="Candice George" height="100" />
      })} */}
    </div>
  )
}
